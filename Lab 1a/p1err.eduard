// lexical errors
// verify if a number is prime

start
let integer 1a // 1a is not permited
read a
let integer i
let boolean isPrime = false
for i from 1 to ( a / 2 )
	if ( a % i ) == 0 then
		isPrime = true
	stop if
stop for
if isPrime == true then
	print "The number is prime ! // missing "
else then
	print "The number isn't prime !"
stop if
stop
